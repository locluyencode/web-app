import React from 'react';
import PropTypes from 'prop-types';
import './styles.scss'

Headers.propTypes = {
    
};

function Headers(props) {
    return (
        <div>
            header
        </div>
    );
}

export default Headers;